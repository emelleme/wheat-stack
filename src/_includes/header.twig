<nav class="navbar bg-base-200" aria-label="Top">
  <div class="navbar-start">
    <div class="dropdown">
      <label tabindex="0" class="btn btn-ghost lg:hidden">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h8m-8 6h16" /></svg>
      </label>
      <ul tabindex="0" class="menu menu-compact dropdown-content mt-3 p-2 shadow bg-base-100 rounded-box w-52">
        {% for item in navigation %}
        <li><a href="{{ item.route }}" class="{{ page.url == item.route ? 'active' : '' }}">{{ item.name }}</a></li>
        {% endfor %}
      </ul>
    </div>
    <a href="/" class="btn btn-ghost normal-case text-xl">
      <img src="/assets/wheat.png" alt="WHEAT Stack logo" class="h-8 mr-2"/>
      <span class="hidden md:inline">WHEAT Stack</span>
    </a>
  </div>
  <div class="navbar-center hidden lg:flex">
    <ul class="menu menu-horizontal px-1">
      {% for item in navigation %}
      <li><a href="{{ item.route }}" class="{{ page.url == item.route ? 'active' : '' }}">{{ item.name }}</a></li>
      {% endfor %}
    </ul>
  </div>
  <div class="navbar-end">
    <input type="checkbox" value="light" class="toggle theme-controller"/>
  </div>
</nav>